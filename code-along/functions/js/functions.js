console.log("functions")

function subtract(x, y) {
	var difference = x - y;
	return difference;
}

function multiply(x, y) {

	// multiple two numbers, store result in product
	var product = x * y;
	return product;
}
